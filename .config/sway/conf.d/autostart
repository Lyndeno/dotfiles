exec steam -silent 
exec discord --start-minimized --disable-frame-rate-limit

# Show volume when adjusted
exec volnoti-dbus

# start notification daemon
exec mako

# start polkit agent
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# start gammastep
exec gammastep-indicator
